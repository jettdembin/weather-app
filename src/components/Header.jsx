import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="header">
      <h1>Weather App</h1>
      {/* Add logo if needed */}
    </header>
  );
};

export default Header;
